package de.t14d3.rapunzellib.visuals;

/**
 * Defines the three axis-aligned planes for 2D shape orientation.
 */
public enum Plane {

    /** The XY plane (Z is the normal). */
    XY,

    /** The XZ plane (Y is the normal). */
    XZ,

    /** The YZ plane (X is the normal). */
    YZ
}
