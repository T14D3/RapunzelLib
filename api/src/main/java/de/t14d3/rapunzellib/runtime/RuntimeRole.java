package de.t14d3.rapunzellib.runtime;

/**
 * The role of the runtime platform.
 */
public enum RuntimeRole {
    /** Running on a game server. */
    SERVER,
    /** Running on a proxy (e.g. Velocity). */
    PROXY
}
